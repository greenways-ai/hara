[package]
name = "hara-vm"
version = "0.1.0"
edition = "2021"
license = "EPL-2.0"
rust-version = "1.78"
description = "VM-only execution surface for verified Hara bytecode"

[dependencies]
hara-wasm = { path = "..", default-features = false, features = ["bytecode-vm"] }
