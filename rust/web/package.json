{
  "name": "@hara-lang/web",
  "license": "EPL-2.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:noir": "vite build --config vite.noir.config.js && vite build --config vite.noir-node.config.js && vite build --config vite.noir-provider.config.js && node package-noir-extension.mjs",
    "build:browser": "vite build --config packages/browser/vite.config.js",
    "build:browser:wasm": "bash ../../scripts/build-hara-browser",
    "test:browser-sdk": "npm run build:browser:wasm && node packages/browser/test.mjs",
    "build:package:noir": "npm run build:noir && cargo run --quiet --manifest-path ../Cargo.toml --bin hara -- package build ../extensions/ledger-noir",
    "test:noir": "npm run build:package:noir && node --test packages/noir/noir.test.mjs noir-loader.test.js",
    "test:hta": "node --test hta.test.js",
    "test:hta:pack": "node test-packed-hta.mjs",
    "test:wasm-runtime": "node --test portable-code-test.test.mjs",
    "test:browser": "playwright test"
  },
  "dependencies": {
    "@hara-lang/hta": "0.1.0",
    "@hara-lang/noir": "0.1.0",
    "@aztec/bb.js": "5.1.0",
    "@noir-lang/noir_js": "1.0.0-beta.25",
    "@noir-lang/noir_wasm": "1.0.0-beta.25"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "fake-indexeddb": "6.2.4",
    "vite": "8.1.5"
  }
}
