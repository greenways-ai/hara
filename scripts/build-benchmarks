#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"

if [[ -f "$ROOT/lib/bench/results/reference-v2.json" ]]; then
  python3 "$ROOT/lib/bench/validate_evidence.py" "$ROOT/lib/bench/results/reference-v2.json"
fi

npm run build --prefix "$ROOT/benchmarks"
