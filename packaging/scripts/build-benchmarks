#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"

if [[ -f "$ROOT/core/lib/bench/results/reference-v2.json" ]]; then
  python3 "$ROOT/core/lib/bench/validate_evidence.py" "$ROOT/core/lib/bench/results/reference-v2.json"
fi

npm run build --prefix "$ROOT/benchmarks"
