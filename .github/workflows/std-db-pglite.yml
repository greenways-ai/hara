name: std.db PGlite WASM

on:
  pull_request:
    branches: [main]
    paths:
      - "core/lib/src/std/db.hal"
      - "core/lib/src/std/db/**"
      - "core/lib/test/std/db/**"
      - "core/java/src/test/java/hara/truffle/HaraDbRuntimeTest.java"
      - "core/java/src/test/java/hara/truffle/HaraPgliteProcessExtensionTest.java"
      - "core/rust/extensions/std-db-pglite/**"
      - "core/rust/web/entries/pglite-*.mjs"
      - "core/rust/web/packages/db-pglite/**"
      - "core/rust/web/pglite-provider.test.mjs"
      - "core/rust/web/vite.pglite-*.config.js"
      - "core/rust/web/package-db-provider.mjs"
      - "core/rust/web/package-pglite-extension.mjs"
      - "packaging/scripts/build-db-pglite-extension"
      - ".github/workflows/std-db-pglite.yml"
  workflow_dispatch:

concurrency:
  group: std-db-pglite-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pglite-wasm:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
          cache-dependency-path: core/rust/web/package-lock.json
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: maven
      - name: Install Hara web build dependencies
        working-directory: core/rust/web
        run: npm ci
      - name: Install pinned PGlite provider
        working-directory: core/rust/extensions/std-db-pglite
        run: npm install --ignore-scripts --no-audit --no-fund
      - name: Build Node provider
        working-directory: core/rust/web
        run: ./node_modules/.bin/vite build --config vite.pglite-node.config.js
      - name: Build browser provider
        working-directory: core/rust/web
        run: ./node_modules/.bin/vite build --config vite.pglite-browser.config.js
      - name: Execute real PGlite provider test
        working-directory: core/rust/web
        run: node --test pglite-provider.test.mjs
      - name: Assemble extension package
        working-directory: core/rust/web
        run: node package-pglite-extension.mjs
      - name: Execute PGlite and database runtimes through Hara
        run: mvn -f core/java/pom.xml -Ptruffle -Dtest=hara.truffle.HaraDbRuntimeTest,hara.truffle.HaraPgliteProcessExtensionTest test
