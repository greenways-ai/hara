name: std.block hard-cut audit

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject removed std.lib.block namespaces and paths
        run: bash scripts/audit-std-block-hard-cut.sh
