name: std.block hard-cut audit

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject retired block namespaces and source paths
        run: bash scripts/audit-std-block-hard-cut.sh
